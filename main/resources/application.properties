# Server settings
server.jetty.acceptors=64
server.jetty.selectors=16
server.connection-timeout=10000
server.port=8070

! AEROSPIKE
aerospike.host=172.19.0.1
aerospike.port=3000

# SWAGGER
# http://<host>:<port>/<service-name>/swagger-ui.html
api.view.enabled=true